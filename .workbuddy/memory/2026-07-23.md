# 2026-07-23

## 音频 URL 前缀更新 + 卡通风格重设计 + 图标集成

### URL 前缀
- 所有音频 URL 从 `/static/audio/xxx.mp4` 改为 `https://env-00jxu1ytdn0v.normal.cloudstatic.cn/white-noise/xxx.mp4`
- 用户自建服务器托管音频

### 图标系统
- 使用 UnoCSS Carbon Icons（项目已有 @iconify-json/carbon）
- 每个声音卡片: rain→cloud-rain, thunder→thunderstorm, waves→humidity, wind→windy, birds→microphone, crickets→bug, fire→flame, sbowl→music, whitenoise→radio, people→cafe
- 底部控制栏: timer, play-filled/pause-filled, user-avatar-filled
- 音量面板: volume-up
- 我的页面: share, screen, sun, trash-can, chevron-right, checkmark

### 卡通风格重设计
- 背景色: #FFF5F2（暖奶油色）
- 声音卡片: 2 列网格，白色圆角 3xl，上方彩色图标圆 + 名称，激活态填满声音专属色
- 底部控制栏: 3 个按钮（定时/播放/我的），白色胶囊 + 绿色大圆播放钮 + 绿色阴影
- 播放按钮: 64px 绿色圆形，carbon 播放/暂停图标
- 音量面板: 带 volume-up 图标 + 滑块 + 百分比
- 定时弹层: checkmark 选中标记
- 每个声音有独立配色:
  rain #6BA5C7, thunder #7B8DBD, waves #5C9BCE, wind #8BAB9E, birds #C9A87C,
  crickets #9BA864, fire #E8945A, sbowl #B8A0D4, whitenoise #A0B4C8, people #C49B7A
- SoundItem 接口新增 icon/color 字段

### 修改文件
- `src/static/sounds.json` — 全部 URL 更新为 cloudstatic CDN，新增 icon + color 字段
- `src/types/sound.ts` — SoundItem 新增 icon/color
- `src/pages/index/index.vue` — 完整卡通风格重写
- `src/pages/me/me.vue` — 完整卡通风格重写，carbon icons

### 构建验证
- `pnpm build:mp-weixin` 成功，零编译错误

## 第二轮重设计 —— 大图标卡片 + 去阴影播放按钮

### 用户反馈
- 上一版图标 h-6 (24px) 装在 h-12 (48px) 圆里太空太小
- 卡片纯白太素，缺少视觉冲击
- 播放按钮绿色 box-shadow 突兀，要求去掉阴影
- 播放按钮 icon "不好搞"，要求复用简洁原始形式

### 改动 (`src/pages/index/index.vue`)
- **卡片背景**: 未选中 = 主题色 14% 透明 (`#color14`)，选中 = 主题色 100% 实色
- **大图标**: h-14 w-14 (56px)，白色或主题色
- **固定高度**: 210rpx 让卡片视觉对齐
- **选中标识**: 4rpx 主题色边框 + 右上角白色半透明对勾圆标
- **播放按钮**: 删除 `box-shadow: 0 4px 16px rgba(7,193,96,0.35)`，纯绿圆 + 白色 play/pause
- **底部三键阴影**: 从 `shadow-lg` 改为 `0 2rpx 12rpx rgba(0,0,0,0.04)` 极淡
- **背景渐变**: 底部栏上方用 `linear-gradient(to top, #FFF8F2 70%, transparent)` 让按钮浮起
- **布局**: 音量面板和底部三键改 `absolute` 浮动，scroll-view 用 `pb-32` 留空间

### 播放按钮 icon
- `i-carbon-play-filled` → `i-carbon-play-filled-alt`（更标准三角形）

### Carbon 图标缺失与替代（已修复）
- `i-carbon-cloud-rain` 不存在 → `i-carbon-rain`
- `i-carbon-bug` 不存在 → `i-carbon-pest`
- `i-carbon-flame` 不存在 → `i-carbon-fire`

### 验证 icon 存在性的小技巧
```bash
node -e "const d=require('node_modules/.pnpm/@iconify-json+carbon@1.2.13/node_modules/@iconify-json/carbon/icons.json'); console.log(Object.keys(d.icons).filter(i=>i.includes('xxx')))"
```
